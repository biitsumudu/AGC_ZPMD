# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "4.0"
metadata:
  name: com.agc.pmd.productrequest
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          url: https://vmsaps4hana.uksouth.cloudapp.azure.com:5207
          client: '100'
        app:
          name: ZBSP_PRDREQUEST
          description: Product Master Request
          package: ZPMD
          transport: S4AK901862
        exclude:
          - /test/
